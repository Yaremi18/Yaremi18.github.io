<article>
  <slot />
</article>

<style>
  article {
    position: relative;
    border-radius: 10px;
    background-color: var(--section-color);
    padding: 40px;
    box-shadow: 6px 6px 10px var(--section-shadow-color);
    overflow: hidden;
  }
</style>
